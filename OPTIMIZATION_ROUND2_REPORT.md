# chjson 第二轮优化与验证报告

日期：2026-09-08。本轮以前一轮完成时的头文件快照为主要基线，并另行复测原始 `f98fc8d`。继续保持 C++17、单头文件和 24 字节 `sv_value`。

## 本轮改动

- 统一五种解析路径的字符串扫描，用 SSE2 有符号比较合并控制字节与非 ASCII 字节检查，减少重复代码与指令；不要求 AVX、SSE4 或 POPCNT，读取始终受输入边界限制。
- 连续 UTF-8 序列集中校验，之后恢复 ASCII 批量扫描；多线程只读解析不再把无转义 UTF-8 字符串误送入复制路径。
- 把转义解码从普通字符串路径中拆出。只读模式批量复制已校验片段；in-situ 模式使用 `memmove` 处理重叠区域，提升开头有转义、后续有大量普通文本的场景。
- 大型平坦数字数组先统计元素数量，再一次分配 DOM 空间；浮点数组同时预留原始输入 backing。遇到嵌套容器或字符串时放弃精确计数，语法仍由正式解析器检查。
- 长字符串数组按估计的 DOM 元素数量预留 arena，避免按文本大小过量预留。
- 自动并行解析默认门槛从 256 KiB 调整为 1 MiB，减少中等大小输入的分片与任务调度开销；仍可通过宏调整。
- 多线程 backing 的切片对齐不再把容量推过缓存上限。固定分片重试后仍不足时，释放分片并退回可扩容的串行 arena，避免有效 JSON 被固定切片预算误判为内存不足。
- 修复解码换行转义后，未结束字符串的错误列号回退不正确，以及末尾反斜杠的 EOF 偏移不一致。新增回归用例先复现失败，再验证修复。

## 编译与正确性验证

| 环境 | 结果 |
|---|---:|
| Windows Clang 17 Release | 6/6 |
| Windows MSVC 19.38 Debug | 6/6 |
| WSL Clang 18 Debug，ASan + UBSan + 泄漏检查 | 6/6 |
| WSL GCC 13 Release，`-Wall -Wextra -Wpedantic` | 2/2 |

**合计 20/20 组 CTest 通过**；GCC 构建无警告，消毒器未报告错误。最终测试包含默认配置、无缓存/无数字快路、可选内存池/SIMD 分片、C-locale 回退、极小多线程分片，以及独立的逐次分配失败注入。

新增覆盖长 ASCII/UTF-8/转义混合文本、SIMD 边界、重叠解码、截断输入与原始行列号、无转义字符串指向 owning buffer、数字数组嵌套回退/尾随输入、冷启动容量上界及强制触发分片不足的回退。保留随机输入变异、各模式回环、深度限制、深层序列化、移动/复用与工作线程异常测试。

原生 Windows Clang 17 的 ASan 运行时在本机无法初始化，因此内存消毒器验证在 WSL Clang 18 上完成。

## 性能测量方法

Windows x64，i9-12900K，Clang 17.0.6，Release `-O3 -DNDEBUG`，C++17，动态 CRT。两版由 CMake 使用同一基准源码、编译器和运行库参数构建。使用亲和性掩码 `0xffff`，两版均限于逻辑 CPU 0–15。每个场景交替先后顺序运行 3 对独立进程，每个进程内部测量 7 次取中位数，再对进程结果取中位数。测试期间没有同时编译本项目。

初步单次进程对照曾出现同一二进制明显波动，因此最终使用上述多进程交替测量。结果只代表本机与这些负载，数个百分点的差异不能视为稳定的普遍收益。原始样本、命令、退出码、环境和头文件 SHA-256 均保存在 JSON 中。

**波动限制：** 多进程复测后，空容器、普通 ASCII 字符串及累计对照中的小对象仍有较大波动。例如同一当前二进制的小对象中位数在两批测量中为 787.5 与 412.7 MiB/s；普通字符串的三个样本为 8038.2、8889.2、11237.7 MiB/s。部分序列化场景也有明显波动，例如当前浮点序列化样本为 4565.0、2170.2、2290.0 MiB/s。下面保留完整结果，但不把这些行的差值作为稳定优化收益或代码回退幅度，也不对本轮序列化提升给出确定结论。转义字符串、UTF-8 字符串和较大对象的解析样本更一致；小对象的历史回退仍未证实消除。

## 相对上一轮的结果

吞吐量单位 MiB/s，变化为本轮相对上一轮。

| 场景 | 解析：上一轮 → 本轮 | 变化 | 序列化：上一轮 → 本轮 |
|---|---:|---:|---:|
| 标量 | 108.8 → 111.6 | +2.6% | 32.0 → 32.1 |
| 单个小对象 | 794.7 → 787.5 | -0.9% | 884.9 → 888.8 |
| 2000 个对象 | 1052.8 → 1043.8 | -0.9% | 1198.3 → 1330.3 |
| 20000 个整数 | 641.7 → 642.3 | +0.1% | 587.1 → 587.4 |
| 20000 个浮点数 | 230.8 → 223.3 | -3.2% | 4469.6 → 2290.0 |
| 5000 个空容器对象 | 281.9 → 433.6 | +53.8% | 447.4 → 520.0 |
| 2000 个 256 字节字符串 | 9060.4 → 8889.2 | -1.9% | 14788.5 → 17830.4 |
| 128 个 4 KiB 字符串对象 | 4219.2 → 11485.6 | +172.2% | 10415.9 → 18132.4 |
| 2000 个含转义的长字符串 | 876.7 → 6171.5 | +603.9% | 2038.5 → 2041.9 |
| 2000 个 UTF-8 字符串 | 1496.9 → 1874.6 | +25.2% | 3341.2 → 18632.1 |
| 约 2 MiB 对象数组 | 5504.2 → 9020.0 | +63.9% | 3651.9 → 3644.8 |
| 约 8 MiB 对象数组 | 1981.3 → 2257.7 | +13.9% | 2907.1 → 3268.3 |

### 冷启动主 arena 容量

| 场景 | 上一轮（字节） | 本轮（字节） | 减少 |
|---|---:|---:|---:|
| 20000 个整数 | 1,524,474 | 480,000 | 68.5% |
| 20000 个浮点数 | 1,980,006 | 810,001 | 59.1% |
| 2000 个 256 字节字符串 | 259,000 | 48,000 | 81.5% |
| 2000 个含转义的长字符串 | 273,000 | 48,000 | 82.4% |
| 2000 个 UTF-8 字符串 | 259,000 | 48,000 | 81.5% |

上述容量来自新线程第一次解析后的 `arena().bytes_committed()`，不是总内存或进程 RSS；不包括输入缓冲区、线程栈、独立 MT backing 和分配器元数据。128 个大字符串对象因并行门槛变化转为串行，存储从独立 MT backing 转到主 arena，因此不能用这一场景的主 arena 数字直接比较总内存。其他保留缓存的复用场景也可能继续持有之前较大输入的容量，可按需要使用既有的 `reset()` / `release_thread_caches()`。

## 相对原始版本的复测

| 场景 | 原始解析 → 当前解析（MiB/s） | 变化 |
|---|---:|---:|
| 标量 | 87.4 → 111.0 | +27.1% |
| 单个小对象 | 752.4 → 412.7 | -45.1% |
| 2000 个对象 | 1107.0 → 995.0 | -10.1% |
| 20000 个整数 | 546.0 → 605.9 | +11.0% |
| 20000 个浮点数 | 160.5 → 212.0 | +32.1% |
| 5000 个空容器对象 | 453.6 → 437.2 | -3.6% |
| 2000 个 256 字节字符串 | 10265.6 → 11326.0 | +10.3% |
| 128 个 4 KiB 字符串对象 | 原始版本崩溃；当前 11818.2 | 无可比速度 |

原始版本的大字符串对象场景持续返回 Windows 访问冲突 `0xC0000005`，没有把崩溃计为低吞吐或计算加速倍数。第一轮历史数据保持在原报告中；这里使用新的同条件复测值。

## 限制与取舍

- 数字数组精确计数增加一次输入扫描，以减少容量浪费和扩容复制；它不保证所有数字分布都有吞吐提升。小对象和部分数字路径仍有回退或波动，以上表格完整列出，不声称全路径加速。
- 多线程切片不足时重试和回退会增加极端不均衡负载的工作量，优先保证可解析性；真实系统分配失败仍可返回 `out_of_memory`。
- 未修改主要序列化算法，也未增加第三方依赖；仍需在调用方真实数据及目标硬件上复测门槛设置。

## 复现与原始记录

```powershell
cmake -S . -B build/round2 -G Ninja -DCMAKE_BUILD_TYPE=Release `
  "-DCMAKE_CXX_COMPILER=D:/Program Files/LLVM/bin/clang++.exe" `
  -DCHJSON_BUILD_TESTS=OFF -DCHJSON_BUILD_BENCHMARKS=ON `
  -DCHJSON_BASELINE_INCLUDE=E:/cc/AI/tokmon/chjson/build/round1/include
cmake --build build/round2
python benchmark/compare_baseline.py `
  --baseline build/round2/chjson_baseline_bench.exe `
  --candidate build/round2/chjson_bench.exe `
  --runs 7 --samples 3 --extended --affinity-mask 0xffff `
  --output benchmark/optimization_round2_results.json
ctest --test-dir build/optimized --output-on-failure
ctest --test-dir build/msvc-debug --output-on-failure
```

上一轮头文件快照保存在当前工作区的 `build/round1/include`，原始版本在 `build/baseline/include`；这些构建目录不纳入 Git。其他机器需指定相应基线目录及可用 CPU 掩码。

- [上一轮对照的全部样本](benchmark/optimization_round2_results.json)
- [原始版本对照的全部样本](benchmark/optimization_cumulative_results.json)
- [第一轮历史报告](OPTIMIZATION_REPORT.md)
